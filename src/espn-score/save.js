/**
 * ESPN Game Score Block - Save Component
 *
 * This is a dynamic block that renders via the Next.js frontend.
 * No HTML is saved to the post content - only the block attributes.
 * The actual scoreboard is rendered by Next.js using the ESPN API.
 */

export default function save() {
  // Dynamic block - content is rendered by Next.js frontend
  return null;
}
