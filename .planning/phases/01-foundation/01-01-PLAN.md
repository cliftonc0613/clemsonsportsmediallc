---
phase: 01-foundation
plan: 01
type: execute
---

<objective>
Establish Clemson Sports Media design system with brand colors and typography.

Purpose: Replace existing Google Fonts with Adobe Fonts (Apotek headlines, Basic Sans body) and add Clemson brand colors as CSS variables. This creates the visual foundation for all subsequent UI work.

Output: Updated layout.tsx with Adobe Fonts, globals.css with Clemson color variables and typography applied.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/STRUCTURE.md

**Key files:**
@frontend/app/layout.tsx
@frontend/app/globals.css

**Design System from PROJECT.md:**
- Primary Orange: #F56600
- Primary Purple: #522D80
- Dark Purple: #2E1A47
- White: #FFFFFF
- Headlines: Apotek (Adobe Fonts)
- Body: Basic Sans (Adobe Fonts)
- Font embed: `<link rel="stylesheet" href="https://use.typekit.net/rlq1tnk.css">`
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integrate Adobe Fonts</name>
  <files>frontend/app/layout.tsx</files>
  <action>
    1. Remove Google Fonts imports (DM_Sans, Playfair_Display, JetBrains_Mono from next/font/google)
    2. Remove the font variable declarations (dmSans, playfair, jetbrains)
    3. Add Adobe Fonts Typekit link in the `<head>` section: `<link rel="stylesheet" href="https://use.typekit.net/rlq1tnk.css" />`
    4. Update body className to remove Google Font variables, keep `font-sans antialiased`
    5. Keep JetBrains_Mono for code blocks if needed, or remove entirely if not used elsewhere

    WHY: Adobe Fonts provides Apotek and Basic Sans which match the sports newspaper aesthetic. Google Fonts don't have these typefaces.
  </action>
  <verify>npm run build succeeds without font-related errors</verify>
  <done>layout.tsx loads Adobe Fonts via Typekit CDN, no Google Font imports remain (except optionally mono for code)</done>
</task>

<task type="auto">
  <name>Task 2: Add Clemson color variables to CSS</name>
  <files>frontend/app/globals.css</files>
  <action>
    1. In the `:root` selector, add Clemson brand color variables:
       ```css
       /* Clemson Sports Media Brand Colors */
       --clemson-orange: #F56600;
       --clemson-purple: #522D80;
       --clemson-dark-purple: #2E1A47;
       --clemson-white: #FFFFFF;
       ```
    2. Update the `--primary` variable to use Clemson orange for CTAs: `--primary: oklch(0.55 0.2 35);` (approximate oklch for #F56600)
    3. Add utility-friendly semantic color mappings in @theme inline section:
       ```css
       --color-clemson-orange: var(--clemson-orange);
       --color-clemson-purple: var(--clemson-purple);
       --color-clemson-dark-purple: var(--clemson-dark-purple);
       ```
    4. Keep existing dark mode colors but ensure they work with new brand colors

    WHY: CSS variables allow consistent color usage across all components and easy theme adjustments.
  </action>
  <verify>Inspect globals.css, confirm new variables are present in :root</verify>
  <done>Clemson brand colors available as CSS variables (--clemson-orange, --clemson-purple, --clemson-dark-purple)</done>
</task>

<task type="auto">
  <name>Task 3: Apply typography with Adobe Fonts</name>
  <files>frontend/app/globals.css</files>
  <action>
    1. Update font-family CSS variables to use Adobe Fonts family names:
       ```css
       --font-sans: "basic-sans", system-ui, sans-serif;
       --font-heading: "apotek", "basic-sans", sans-serif;
       ```
    2. Update heading styles in @layer base to use --font-heading:
       ```css
       h1, h2, h3, h4, h5, h6 {
         font-family: var(--font-heading);
       }
       ```
    3. Ensure body text uses --font-sans (Basic Sans)
    4. Update fluid typography weights if needed - Apotek should be bold/black for headlines
    5. Update comment at top of file to reflect new fonts (remove "DM Sans, Playfair Display" reference)

    WHY: Apotek's condensed bold style creates the sports newspaper headline aesthetic. Basic Sans provides clean readability for body content.
  </action>
  <verify>npm run dev, inspect page in browser to confirm fonts are loading (check Network tab for typekit.net requests, inspect computed styles on headings)</verify>
  <done>Headlines render in Apotek font family, body text in Basic Sans, fonts visibly different from previous Google Fonts</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds without errors
- [ ] `npm run dev` starts without errors
- [ ] Browser Network tab shows successful request to `use.typekit.net/rlq1tnk.css`
- [ ] Inspecting an h1 element shows `font-family: apotek` in computed styles
- [ ] Inspecting body text shows `font-family: basic-sans` in computed styles
- [ ] CSS variables `--clemson-orange`, `--clemson-purple`, `--clemson-dark-purple` exist in :root
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No TypeScript or build errors
- Adobe Fonts loading successfully from Typekit CDN
- Clemson brand colors available as CSS variables
- Typography visually reflects Apotek headlines and Basic Sans body
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md` with:
- Accomplishments (fonts integrated, colors added)
- Files modified (layout.tsx, globals.css)
- Decisions made (kept/removed mono font, oklch color conversions)
- Issues encountered (if any)
- Next step: Ready for Phase 2 (Hero Grid) or additional foundation work
</output>
